# 🌐 Trading Proxy Setup

> Автоматическая настройка двухзвенного Shadowsocks прокси для трейдинга с минимальным пингом до биржи.

---

## ⚠️ Важно — географическое положение

Данная схема прокси имеет смысл **только при определённом географическом расположении**.  
Идея в том, чтобы трафик шёл через сервер, физически близкий к бирже — это снижает задержку.

Пример для трейдеров из **России**:
- Транзитный сервер — **Хабаровск** (оптимизирует маршрут внутри страны до точки выхода)
- Exit сервер — **Токио** (оптимизирует маршрут от точки выхода до биржи)

Если ты находишься в другом регионе — подбирай города серверов исходя из своего местоположения и расположения серверов биржи.

---

## 📡 Схема работы

```
Твой ПК  (Shadowsocks клиент)
    ↓
Транзитный сервер — город рядом с тобой  (Glider в Docker)
    ↓
Exit сервер — город рядом с биржей  (Shadowsocks-libev)
    ↓
Биржа (Binance / Bybit / др.)
```

---

## 🚀 Использование

### Шаг 1 — Exit сервер (город рядом с биржей)

Подключись по SSH и выполни:

```bash
wget https://raw.githubusercontent.com/iamRIOS/proxyFORtrading/main/setup_exit_server.sh
chmod +x setup_exit_server.sh
./setup_exit_server.sh
```

Скрипт спросит порт, пароль и метод шифрования — и выведет итоговые параметры для следующего шага.

---

### Шаг 2 — Транзитный сервер (город рядом с тобой)

Подключись по SSH и выполни:

```bash
wget https://raw.githubusercontent.com/iamRIOS/proxyFORtrading/main/setup_transit_server.sh
chmod +x setup_transit_server.sh
./setup_transit_server.sh
```

Скрипт спросит IP exit сервера из шага 1, пароль и порты — и настроит Glider в Docker.

---

### Шаг 3 — Shadowsocks клиент (Windows)

Скачай клиент: [shadowsocks-windows releases](https://github.com/shadowsocks/shadowsocks-windows/releases)

Параметры подключения:
| Поле | Значение |
|------|----------|
| **Server** | IP транзитного сервера |
| **Port** | 8388 (или тот что задал) |
| **Password** | твой пароль |
| **Encryption** | aes-256-gcm |

---

## 📋 Требования

- Ubuntu 20.04 или новее
- Root доступ на обоих серверах

---

## 🔧 Полезные команды

```bash
# Статус shadowsocks на exit сервере
systemctl status shadowsocks-libev

# Логи Glider на транзитном сервере
docker logs proxy

# Перезапустить Glider
docker restart proxy

# Сменить exit сервер на транзитном (если переехал на новый VPS)
docker stop proxy && docker rm proxy
docker run -d --name proxy --restart unless-stopped --network host \
  nadoo/glider -verbose \
  -listen "ss://AEAD_AES_256_GCM:ПАРОЛЬ@:8388" \
  -forward "ss://AEAD_AES_256_GCM:ПАРОЛЬ@НОВЫЙ_IP:8388"
```
