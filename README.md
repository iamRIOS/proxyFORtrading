<div align="center">

# <a href="https://github.com/iamRIOS/">iamRIOS</a><a href="https://github.com/iamRIOS/proxyFORtrading">/proxyFORtrading</a>

</div>

Двухзвенный Shadowsocks прокси для трейдинга с минимальным пингом до биржи.  
Оптимизирован для работы с **Binance**, **Bybit** и другими криптобиржами через маршрут Россия → Хабаровск → Япония.

> [!WARNING]
> ### БЕЗОПАСНОСТЬ
> Никогда не используйте непроверенные прокси-сервера для торговли.  
> Всегда проверяйте, что вы запускаете именно этот скрипт, а не его модифицированную версию.

> [!IMPORTANT]
> Скрипты предназначены для **Ubuntu** с root-доступом.
> Установка имеет смысл в зависимости от вашего местоположения.

---

## 🗺️ Схема работы

```
Твой ПК (Shadowsocks клиент)
        ↓
Транзитный сервер — Хабаровск (glider в Docker)
        ↓
Exit сервер — Япония (shadowsocks-libev)
        ↓
Binance / Bybit / биржа
```

Такая схема позволяет получить **минимальный пинг до японских серверов бирж**, используя географически близкий выходной узел, при этом сохраняя удобное подключение из России через транзитный сервер в Хабаровске.

---

## ⚙️ Использование

### Шаг 1 — Exit сервер (Япония)

Подключись к серверу по SSH и выполни:

```bash
wget https://raw.githubusercontent.com/iamRIOS/proxyFORtrading/main/setup_exit_server.sh
chmod +x setup_exit_server.sh
./setup_exit_server.sh
```

Скрипт запросит **порт**, **пароль** и **метод шифрования**, после чего выведет итоговые параметры подключения.

---

### Шаг 2 — Транзитный сервер (Хабаровск)

Подключись к серверу по SSH и выполни:

```bash
wget https://raw.githubusercontent.com/iamRIOS/proxyFORtrading/main/setup_transit_server.sh
chmod +x setup_transit_server.sh
./setup_transit_server.sh
```

Скрипт запросит **IP exit сервера**, **пароль** и **метод шифрования** — и автоматически развернёт glider в Docker.

---

### Шаг 3 — Клиент на Windows

Скачай Shadowsocks для Windows: [shadowsocks-windows/releases](https://github.com/shadowsocks/shadowsocks-windows/releases)

Параметры подключения:

| Параметр | Значение |
|---|---|
| **Server** | IP транзитного сервера (Хабаровск) |
| **Port** | порт (по умолчанию `8388`) |
| **Password** | твой пароль |
| **Encryption** | `aes-256-gcm` |

---

## ℹ️ Описание скриптов

- [**`setup_exit_server.sh`**](./setup_exit_server.sh) — настройка выходного сервера в Японии.  
  Устанавливает и конфигурирует `shadowsocks-libev`. Принимает порт, пароль и метод шифрования.

- [**`setup_transit_server.sh`**](./setup_transit_server.sh) — настройка транзитного сервера в Хабаровске.  
  Разворачивает `glider` в Docker, который принимает входящий Shadowsocks-трафик и пробрасывает его на exit сервер в Японии.

---

## 🔧 Полезные команды

```bash
# Статус shadowsocks на exit сервере
systemctl status shadowsocks-libev

# Логи glider на транзитном сервере
docker logs proxy

# Перезапустить glider
docker restart proxy

# Сменить exit сервер на транзитном (новый IP)
docker stop proxy && docker rm proxy
docker run -d --name proxy --restart unless-stopped --network host \
  nadoo/glider -verbose \
  -listen "ss://AEAD_AES_256_GCM:ПАРОЛЬ@:8388" \
  -forward "ss://AEAD_AES_256_GCM:ПАРОЛЬ@НОВЫЙ_IP:8388"
```

---

## ☑️ Распространённые вопросы и проблемы

### Скрипт выполняется, но прокси не работает

- Убедитесь, что порт `8388` (или выбранный вами) открыт в фаерволе на обоих серверах.
- Проверьте, что Docker запущен на транзитном сервере: `systemctl status docker`
- Проверьте логи: `docker logs proxy`

### Высокий пинг до биржи

- Убедитесь, что exit сервер находится в Японии и как можно ближе к дата-центру биржи (Tokyo).
- Проверьте пинг напрямую с exit сервера до биржи: `ping api.binance.com`

### glider не запускается в Docker

- Убедитесь, что Docker установлен: `docker --version`
- Установите Docker при необходимости: `curl -fsSL https://get.docker.com | sh`

### Не удаётся подключиться с Windows-клиента

- Проверьте, что метод шифрования в клиенте совпадает с тем, что задан при настройке (`aes-256-gcm`).
- Убедитесь, что используете IP **транзитного** сервера (Хабаровск), а не exit сервера (Япония).

---

## 📋 Требования

- Ubuntu
- Root-доступ на обоих серверах
- Docker (для транзитного сервера — устанавливается автоматически или вручную)

---

## ⭐ Поддержка проекта

Если репозиторий оказался полезным — поставь ⭐ этому репозиторию (сверху справа этой страницы)
